services:
  avimind:
    build: .
    container_name: avimind
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - AVIMIND_DB_URL=sqlite:////app/data/avimind.db
    restart: unless-stopped